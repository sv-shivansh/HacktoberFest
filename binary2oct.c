#include<stdio.h>
long long binoct(long long n);
{
    printf("Enter the binary number : ");
    scanf("%lld",&n);
    printf("Binary number demimal value is %lld ",binoct(n));
    printf("Binary number octahedral value is %lld",binoct(n));
return 0;    
}
long long biooct(long long n);
{
    
}
